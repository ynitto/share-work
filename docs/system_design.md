# 分散AIエージェントタスク分担システム設計

## 🧩 システム概要

**目的**：GitLab を“メールボックス”として使い、分散環境に配置された CLI 型 AI エージェント（常駐プログラム）が自律的にタスクを分担・完遂する仕組みを構築する。

- タスク発注側は自然文で要件を投げる
- 発注側常駐プログラムがタスクを分解し GitLab にプッシュ
- 受注側常駐プログラムが GitLab をポーリングしてタスクを受注
- 受注側は CLI 型 AI エージェントを動かして成果物を生成し GitLab に返す
- 発注側は成果物を取得しタスク関連ファイルをクリーンアップ

---

## 🗂️ リポジトリ構成（メールボックスとしてのGitLab）

```
/tasks/
  <task-id>/
    meta.yaml        # ステータス / 担当 / リソース要件
    requirements.txt # 自然文要件
    workplan.md      # 手順・プロンプト・スキル案
    artifacts/       # 成果物（コード/ドキュメント）
/logs/             # 任意（デバッグ用ログ）
/workers/          # 任意（Worker状態など）
```

---

## 🔄 タスクフロー（エンドツーエンド）

### 1) 発注（自然文入力）
- 発注者が発注ノードの常駐プログラムに自然文で要件を投げる
- 常駐プログラムがタスクを分解し、`tasks/<task-id>/` を作成して GitLab にプッシュ

### 2) 受注（ポーリング & 受注）
- 受注側ノードは GitLab を定期ポーリング
- 新規タスクを検知したらリソース状況を評価し、受注可否を決定
- 受注すると `meta.yaml` を更新して `status: claimed` にし、コミット・プッシュ

### 3) 実行（AIエージェント呼び出し）
- 受注したタスクを CLI 型 AI エージェントに渡し、成果物を `artifacts/` に生成
- 成果物作成完了後、`meta.yaml` を `status: done` に更新してプッシュ

### 4) 完了回収 & クリーンアップ
- 発注側の常駐プログラムは完了タスクを検知し成果物を取得
- タスク関連ファイルを削除してリポジトリをクリーンに保つ

---

## 🧠 タスクメタ例（meta.yaml）

```yaml
task_id: task-20260313-001
created_at: "2026-03-13T12:00:00Z"
status: open            # open / claimed / in_progress / done / failed
requested_by: userA
assigned_to: null       # worker_id（CLAIM時に埋まる）
priority: normal
resources:
  cpu: 2
  memory: 2048
  disk: 1024
```

---

## 🛠️ 主要コンポーネント

### Controller（発注側常駐プログラム）
- 要件受信 → タスク分解 → GitLab push
- 完了タスクの検知・成果物取得・クリーンアップ

### Worker（受注側常駐プログラム）
- GitLab ポーリング → 受注判定 → タスク実行 → 結果報告
- CLI エージェント実行・エラーハンドリング

### CLI 型 AI エージェント
- 各ノードにインストール済み
- 受注タスクを受け取り成果物を生成

---

## ⚠️ リスク/対策（運用上の注意）

### レースコンディション（タスク取り合い）
- `meta.yaml` 更新時は常に `git pull` → `commit` → `push` を行い、競合を解消
- `claimed` 状態にタイムアウトを設け、一定時間経過でも続行しない場合は再公開

### 中断・失敗のリカバリ
- `in_progress` にタイムスタンプを保持し、長時間放置のタスクは再公開
- 失敗時は `status: failed` / `artifacts/error.log` を残す

### セキュリティ/安全性
- GitLab のアクセストークン・ユーザ制御で悪意あるタスクを制限
- 受注側で「許可済みタスク種類」「禁止コマンド」を設ける

---

## 🧪 拡張案

- Web UI（タスク一覧・進捗管理）
- WebHook / イベント駆動（ポーリングの代替）
- リソース可視化（各Workerの稼働状況メトリクス）
- 複数タスクの並列実行管理
